// Ambient declaration for global CSS side-effect imports (e.g. `import './styles.css'`).
// Next 16's bundled types cover this; Next 15's do not, so declare it explicitly.
declare module '*.css'
declare module '*.scss'
declare module '*.sass'
